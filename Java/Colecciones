Listas. (List)
Es una colección de objetos donde cada uno de ellos lleva un índice asociado. Permite repetición de elementos. Se pueden insertar y eliminar elementos en las posiciones intermedias.

Conjunto. (SET)
Es una colección de objetos que no admite duplicados.
No es posible tener información sonbre el orden de los elementos.
Los set se recooren con iteradores.

Map.
Colección que mapea claves y valores, no puede tener claves repetidas.

Cola (Queue)
Implementadas con el algoritmo -> FIFO Primero en entrar primero en salir

Pila (Stack)
Implementadas con el algoritmo FILO Primero en llegar último en salir
